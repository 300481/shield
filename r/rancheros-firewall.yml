rancheros-firewall:
  image: 300481/rancheros-firewall:0.1.1
  command: "/bin/configure-firewall.sh"
  privileged: "true"
  labels:
    io.rancher.os.scope: "system"
    io.rancher.os.after: "docker"
  volumes_from:
    - all-volumes
  restart: always
  net: host