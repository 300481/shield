rancheros-firewall:
  image: 300481/rancheros-firewall:latest
  command: "/bin/configure-firewall.sh"
  privileged: "true"
  labels:
    io.rancher.os.scope: "system"
    io.rancher.os.after: "docker"
  environment:
  - PUBLIC=FALSE
  volumes_from:
    - all-volumes
  restart: always
  net: host